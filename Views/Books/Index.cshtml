@{
    ViewBag.Title = "Books";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Books</h2>

<p>
    <a class="btn btn-primary" href="Books/New">New Book</a>
</p>

<table id="tableforbooks" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Book</th>
            <th colspan="2">Genre</th>
        </tr>
    </thead>
    <tbody>
    </tbody>
</table>
@section Scripts{
    <script>
        $(document).ready(function () {
            /* $.get("/api/Books", function (data) {
                 for (let i = 0; i < data.length; i++) {
                     $("#tableforbooks")
                         .append("<tr> < td > <a href='Edit'>{0}</a></td ><td>{1}</td><td><a href='Details'>Details</a></td></tr>".replace("{0}", data[i].name).replace("{1}", data[i].genre.name))
                 }

             }, "json");
             */
            $("#tableforbooks").DataTable({
                ajax: {
                    url: "api/books",
                    dataSrc: ""
                },
                columns: [
                    {
                        data: "name",
                        render: function (data, type, book) {
                            return "<a href='books/details/" + book.id + "'>" + book.name + "</a>";
                        }
                    },
                    {
                        data: "genre.name"
                    },
                ]
            });
        })

    </script>



}
